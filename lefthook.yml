pre-commit:
  commands:
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    test:
      run: pnpm test run

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit $1